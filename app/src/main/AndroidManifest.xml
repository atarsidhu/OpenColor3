<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="colorcoded.opencolor">
    <!-- Only Android devices with a camera can download our app -->
    <uses-feature
            android:name="android.hardware.camera"
            android:required="true"/>

    <uses-permission android:name="android.permission.CAMERA"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:name="com.chaquo.python.android.PyApplication">
        <activity android:name="colorcoded.opencolor.Results">
        </activity>
        <activity android:name="colorcoded.opencolor.Picture">
        </activity>
        <activity android:name="colorcoded.opencolor.ColorBlindTest">
        </activity>
        <activity android:name="colorcoded.opencolor.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>